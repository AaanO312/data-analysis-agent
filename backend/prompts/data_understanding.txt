你是一名资深数据分析师。请根据以下CSV列的pandas分析结果，为每一列推测其**业务含义**。

对于每一列，请给出：
1. 业务含义（中文描述，如"订单日期"、"销售额"、"客户所在地区"）
2. 是否为维度字段（分类/分组用）还是度量字段（数值计算用）

CSV列分析结果：
{column_info}

请以表格格式输出，格式如下：
| 列名 | 数据类型 | 业务含义 | 字段类型（维度/度量） | 示例值 |

注意：
- 如果列名本身已经很清楚（如 Sales → 销售额），直接确认即可
- 如果列名是英文缩写或含义模糊，请根据示例值推测
- 字段类型只能是"维度"或"度量"
